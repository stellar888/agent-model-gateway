# Task 1 — Scaffold the Repository

Read `AGENTS.md`, `README.md`, and `specs/acceptance-criteria.md`.

Create the Python project structure, `pyproject.toml`, `.gitignore`, `.env.example`, application packages, test directories, and placeholder documentation files.

Use:

- Python 3.12+
- FastAPI
- Pydantic v2
- httpx
- PyYAML
- Typer
- pytest
- Ruff
- mypy
- OpenAI Python SDK

Requirements:

1. Configure editable installation.
2. Add a console entry point named `agent-poc`.
3. Configure Ruff and mypy.
4. Create a minimal `/health` endpoint.
5. Add one health endpoint test.
6. Ensure imports and tests run.
7. Do not implement the gateway yet.

Run the available quality checks and report the exact commands and outcomes.
